# Project Rules

- **Git Push Policy**: Do NOT push code changes to GitHub (`git push`) under any circumstances until the user has tested the local changes and explicitly requested to push. All commits and modifications must remain local until approved for push.
