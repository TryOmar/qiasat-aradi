# دليل استخدام مكوّن الكسور الموحد (FractionHelper v2.0.0)

مكوّن جافا سكربت وCSS مشترك ومستقل تماماً، مصمم لتطبيق **الدَّلاَّل** لتوحيد عملية إدخال الكسور العشرية والزراعية، وكشف الأخطاء الشائعة ميدانياً، وترجمة القياسات للعربية المكتوبة الفورية.

---

## 📂 ملفات المكوّن
* **الجافا سكربت المشترك:** `fraction-helper.js` (في المجلد الرئيسي).
* **التنسيق المشترك:** `fraction-helper.css` (في المجلد الرئيسي).

---

## 🚀 طريقة الاستدعاء والدمج

لدمج المساعد في أي صفحة/برنامج داخل التطبيق، اتبع الخطوات التالية:

### 1. ربط الملفات في رأس الصفحة (HTML)
أضف رابط التنسيق داخل وسم `<head>`:
```html
<link rel="stylesheet" href="../../fraction-helper.css" />
```

أضف رابط ملف الجافا سكربت في أسفل جسم الصفحة وقبل ملفات الملاحة:
```html
<script src="../../fraction-helper.js"></script>
```

### 2. التهيئة عند تحميل الصفحة
قم بتهيئة المكوّن باستدعاء الدالة التالية:
```html
<script>
  document.addEventListener("DOMContentLoaded", () => {
    FractionHelper.init({
      // خيارات التهيئة (اختياري)
    });
  });
</script>
```

---

## ⚙️ خيارات التهيئة (Configuration Options)

يمكن تخصيص سلوك المساعد بتمرير كائن خيارات لدالة `init()`:

| الخيار | النوع | القيمة الافتراضية | الوصف |
| :--- | :--- | :--- | :--- |
| `selector` | `string` | `input[inputmode="decimal"]...` | محدد الاستعلام عن الحقول المستهدفة بالمراقبة التلقائية. |
| `showTranslation` | `boolean` | `true` | إظهار أو إخفاء ترجمة القياس الفورية للغة العربية. |
| `showTypoDetector` | `boolean` | `true` | تفعيل/إيقاف كاشف الأخطاء الشائعة (مثل كتابة .83 بدلاً من .833). |
| `showQuickPills` | `boolean` | `true` | إظهار أو إخفاء شريط الكسور السريعة (⅛, ¼, ½, ⅓...). |
| `preferredPosition` | `string` | `'auto'` | موضع التوليب المفضل: `'auto'` (تلقائي) أو `'above'` (أعلى الحقل) أو `'below'` (أسفل الحقل). |
| `devMode` | `boolean` | `false` | تفعيل وضع التطوير لقياس زمن المعالجة ومراقبة الأداء في الـ Console. |
| `lang` | `string` | `'ar'` | لغة ترجمة النصوص (مهيأة للتعديل والترجمة للغات أخرى). |

---

## 📥 واجهة البرمجيات العامة (Public API)

يوفر كائن `window.FractionHelper` مجموعة دوال تفاعلية للتحكم بالمساعد:

### 1. `FractionHelper.init(options)`
يهيئ المكوّن ويحقن التوليب المساعد في الـ DOM ويبدأ مراقبة التغيرات والحقول. يمنع هذا التابع التهيئة المكررة تلقائياً.

### 2. `FractionHelper.destroy()`
ينظف الصفحة كلياً؛ يزيل التوليب من الـ DOM، ويفك ارتباط جميع مستمعات الأحداث والـ MutationObserver، ويصفر المراجع الذاكرية لمنع تسرب الذاكرة (Memory Cleanup).

### 3. `FractionHelper.refresh()`
إعادة حساب تموضع التوليب وتحديث المحتوى فوراً للحقل النشط حالياً.

### 4. `FractionHelper.attach(element)`
إرفاق المساعد الطائر يدوياً بحقل قياس محدد خارج نطاق المراقبة التلقائية الافتراضي.

### 5. `FractionHelper.detach(element)`
إلغاء إرفاق المساعد وفصل حقل محدد يدوياً.

---

## 💡 أفضل الممارسات (Best Practices)
1. **العمل بدون اتصال (Offline First):** المكوّن مستقل كلياً ولا يحمل أي ملفات أو خطوط خارجية، مما يضمن عمله بنسبة 100% دون اتصال.
2. **منع التكرار:** لا تقم بالتهيئة المتعددة يدوياً؛ المساعد يستخدم تفويض الأحداث (Event Delegation) و MutationObserver لمراقبة الحقول الديناميكية تلقائياً.
3. **وضع التطوير (DevMode):** فعّل خيار `devMode: true` أثناء التطوير لمراجعة الأداء وسرعة الاستجابة وعدد الحقول المرتبطة بالصفحة.

---

## 🛠️ سياسة الصيانة طويلة المدى (Maintenance Mode)

اعتباراً من الإصدار **Release 2026.1**، يدخل المكوّن مرحلة الصيانة المستقرة، ويخضع للسياسات التالية:

### 1. الحفاظ على التوافق الخلفي (Backward Compatibility)
أي تعديل أو تطوير مستقبلي يجب ألا يغير المعادلات الرياضية أو النتائج الحسابية أو منطق الرسم الهندسي للبرامج الأربعة المدمج بها حالياً.

### 2. ترقيم الإصدارات الجديدة (Semantic Versioning)
يمنع تعديل الإصدار `v2.0.0` الحالي مباشرة. عند الحاجة للتطوير:
* ترقية الرقم الثالث لإصلاح الأخطاء الطفيفة (مثل `v2.0.1`).
* ترقية الرقم الثاني لإضافة خيارات تهيئة أو ميزات صغيرة (مثل `v2.1.0`).
* ترقية الرقم الأول للتغييرات الكبيرة الجوهرية (مثل `v3.0.0`).

### 3. متطلبات الاعتماد قبل النشر
قبل نشر أي تحديث مستقبلي على GitHub، يجب تشغيل واجتياز اختبارات الأداء والانحدار (Regression Tests) بنسبة **100%** لضمان استقرار العمليات الحسابية والرسومات الهندسية على الهواتف والأجهزة اللوحية دون اتصال بالإنترنت.

